# openwrt-samba-nas-raspberrypi
Step-by-step configuration of a Raspberry Pi running OpenWrt to function as a LAN-accessible network-attached storage (NAS) using Samba. Covers identifying, mounting, and sharing storage from SD card partitions, directories on the root filesystem, USB flash drives, or external hard drives over the local network.
